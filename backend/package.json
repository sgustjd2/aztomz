{
  "name": "aztomz-backend",
  "private": true,
  "type": "module",
  "description": "한끗 백엔드 스크립트 — 발행 자동화에만 playwright 가 필요하다(프론트엔드는 여전히 의존성 0).",
  "scripts": {
    "postinstall": "playwright install chromium",
    "login": "node scripts/blog-publish.mjs --login",
    "categories": "node scripts/blog-publish.mjs x --categories",
    "category:new": "node scripts/category-new.mjs"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  }
}
