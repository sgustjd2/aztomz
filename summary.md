# 한끗(az2mz) — 신뢰도 채점 · 검증 · 수집 로직 정리

> 출처: `hermes/SOUL.md`, `hermes/skills/hangeut/*/SKILL.md` (2026-07-11 기준)
> 이 문서는 "고구미봇"이 실제로 참조하는 프롬프트/규칙 원문을 정리한 것입니다.

---

## 0. SOUL.md — 고구미봇 페르소나

```
당신은 '고구미봇', 한끗(트렌드 번역소)의 전속 분석가입니다.
요즘 유행하는 디저트·식당·카페·신조어·노래·패션·AI까지 1분 만에 이해시키고,
SNS에서 보이는 게 "광고인지 진짜인지"를 가려주는 게 사명입니다.
```

**핵심 가치 4개**
| 가치 | 내용 |
|---|---|
| 최신성 집착 | "이게 지금 진짜 뜨는가?"를 늘 의심·검증. 옛 유행(지브리풍 AI, 약과 등)을 '신상'으로 포장하지 않음 |
| 거품 간파 | SNS 노출량 ≠ 좋음. 광고·협찬·댓글 품앗이를 진짜 후기와 분리 |
| 정직 | 모든 판단은 추정치임을 밝히고 단정하지 않음. 세대 비하 없이 |
| 자기개선 | 틀린 판정·피드백을 `learnings.md`에 기록해 다음 분석부터 반영 — "매번 어제보다 정확해지는 것"이 목표 |

**말투**: 핵심부터, 명료하고 친근하게. 과한 텐션·이모지 남발 안 함. 모르면 모른다고 함.

**일하는 법**: 작업 시작 전 `learnings.md`를 반드시 먼저 읽고 과거 교훈 적용 → 새로 배운 것은 한 줄로 기록.

---

## 1. 세 가지 점수, 왜 나누는가

한끗은 하나의 "인기 점수" 대신 **세 개의 독립 축**을 매깁니다.

| 축 | 의미 | 값 |
|---|---|---|
| `ad` 광고 의심도 | 이게 얼마나 "마케팅처럼" 보이나 | 0~100 |
| `trust` 후기 신뢰도 | 리뷰가 실제 경험 기반으로 믿을 만한가 | 0~100 |
| `sat` 만족도 | 실제로 다녀온 사람들이 좋았다고 하는가 | pos / neg / mix |

세 축을 분리하는 이유를 보여주는 실제 사례(두바이 초콜릿 편의점판):
`ad 57 · trust 57 · sat neg` — "후기는 믿을 만한데, 그 후기가 '별로'라고 말한다." 신뢰도가 높다고 만족도가 좋다는 뜻이 아님을 그대로 보여주는 케이스.

---

## 2. 광고 의심도(`ad`) — 측정 방법

평가 주체: `hangeut-review`(검수 에이전트, `.pipeline/collect.json` → `.pipeline/review.json`).

### 최강 신호 2개 (사용자 지정 — 이 중 하나만 확인돼도 신호 개수 무시하고 즉시 '높음(61+)'권)

**① 협찬·대가성 흔적**
- 공정위 의무고지·대가성 문구가 본문에 그대로 보이면: `"소정의 원고료를 지원받아"`, `"업체로부터 제품/서비스를 제공받아 작성"`, `"협찬받아"`, `"체험단/서포터즈"`, `"유료광고 포함"`, `"파트너스 활동의 일환으로 수수료를 제공받습니다"`
- 협찬성 해시태그: `#협찬 #제공 #광고 #체험단 #서포터즈 #AD`
- 모순 신호: "내돈내산"을 강조하면서 위 협찬 마커가 같이 보이면 **오히려 가점**

**② 후기 품앗이 · 바이럴 작업**
- 본문에서 관찰 가능(자동 가점 OK): 여러 블로그가 거의 동일한 문장·구성·사진 순서를 복붙한 듯 반복(템플릿형 원고 배포), 같은 시기 무더기 발행, 동일 표현("인생맛집", "무조건 가야") 반복
- 인스타 인게이지먼트 팟(계정끼리 상호 댓글·좋아요): 직접 스크래핑 금지(ToS)라 확인 불가 → **"품앗이 의심" 플래그만**, 최종 가중은 사람 검수

### 기타 가점 신호
- 오픈 직후 1~2주 단기 집중 노출
- 인플루언서(팔로워 많은 계정) 중심 확산
- 맛·가격·웨이팅 등 구체 경험 없이 분위기 칭찬만
- 지도 리뷰는 적은데 SNS만 핫함(지도-SNS 괴리)
- 부정 후기가 거의 없이 과하게 긍정적

### 점수 변환 공식 (신호 개수 → 점수 구간)
| 신호 개수 | 점수 구간 |
|---:|---|
| 0개 | 0~20 |
| 1~2개 | 21~40 |
| 3~4개 | 41~60 |
| 5개 | 61~80 |
| 6~7개 | 81~100 |

> 단, **명백한 협찬 고지 또는 템플릿형 품앗이 정황이 확인되면 신호 개수와 무관하게 '높음(61+)'권으로 강제 가중.** 근거(`reasons.ad`)에 실제로 본 협찬 문구/품앗이 정황을 구체적으로 남긴다.

---

## 3. 후기 신뢰도(`trust`) — 측정 방법

가점 신호(구체적일수록 신뢰 ↑):
- 구체적인 맛 표현(달다·짜다·느끼하다·식감 등)
- 가격 만족/불만족이 다양하게 존재
- 웨이팅 경험 언급(시간대별 정보)
- 재방문 의사("또 갈 듯", "다음엔 다른 메뉴")
- 단점 언급(장점만 아니라 단점도 — 균형 잡힌 리뷰일수록 신뢰 ↑)
- 일반 사용자 계정 비중(팔로워 적은 일반 계정·지도 리뷰)
- 리뷰 발생 기간이 특정 시기에 몰리지 않고 분산

점수 변환은 `ad`와 동일한 공식(신호 0개=0~20 … 6~7개=81~100)을 적용.

---

## 4. 만족도(`sat`) — 별개 축

`pos | neg | mix` 세 값만 가진다. **후기 신뢰도(믿을 만한가)와 다른 축**임을 반드시 지킨다 —
신뢰도가 높아도 내용이 "별로"면 `neg`.

`flag` 파생값: `광고거품`(광고의심 높음 + 신뢰 낮음) / `저신뢰`(데이터 부족) / `정상`.

---

## 5. 최종 라벨 조합 (ad/trust → 사용자에게 보이는 한 줄)

원안(PRD 9.7, 세밀 8단계):
| 광고 의심도 | 후기 신뢰도 | 최종 라벨 |
|---|---|---|
| 낮음 | 높음 | 믿고 가볼 만함 |
| 중간 | 높음 | 광고도 있지만 실제 만족도 있음 |
| 높음 | 높음 | 바이럴 강하지만 실후기도 좋은 편 |
| 높음 | 낮음 | 광고 가능성 높음, 방문 전 주의 |
| 낮음 | 낮음 | 아직 데이터 부족 |
| 중간 | 보통 | 취향 따라 갈림 |
| 낮음 | 보통 | 조용히 뜨는 중 |
| 높음 | 보통 | SNS 기대치 조절 필요 |

실제 구현(`hangeut-curate` 단순화 버전, `labelCls` 포함):
- 낮은 광고 + 높은 신뢰 → **「믿고 가볼 만함」**(`good`)
- 높은 광고 + 낮은 신뢰 → **「광고 가능성 높음, 방문 전 주의」**(`warn`)
- 그 외 혼재 → **「취향 따라 갈림」** 또는 **「SNS 기대치 조절 필요」**(`mid`)

모든 점수 옆에는 `reasons.{ad,trust,sat}` — "왜 이 점수인지" 근거 1문장이 상세페이지에 그대로 노출된다 (예: "출시 직후 협찬 해시태그·반복 문구 집중 → 중상").

---

## 6. "추측"의 원리 — 3단계 검증 게이트

점수를 매기는 것과, 그 점수가 **진짜인지 재확인하는 것**은 서로 다른 에이전트가 담당한다(적대적 검증 구조).

### 6-1. 최신성 검증 — `hangeut-verify`
"이게 진짜 지금도 뜨고 있나?"를 독립적으로 다시 검색한다.
- 검색: `"[트렌드명] 2026년 6월"`, `"[트렌드명] 요즘"`, `"[트렌드명] 유행 끝/한물"`
- 유튜브: 상위 영상이 **최근 게시 + 조회수 급상승**인가, 아니면 오래된 영상(6개월+)만 있나
- 교차 판단: 상승 신호(최근 글 多·급상승 조회수) vs 하락 신호(최근 글 적음·"끝물" 언급·오래된 영상만)
- 판정: `진짜 최신`(통과) / `한물감`(→ `drop:true` 권고, stage를 '끝물'로 강등) / `애매`(데이터 부족, 통과 보류)
- 한물감으로 판명되면 `learnings.md`에 `- [날짜] (최신성) ○○는 한물감 — 더 넣지 말 것` 한 줄 기록 → 다음부터 자동 차단

### 6-2. 출처 검증 — `hangeut-source`
"200 OK ≠ 진짜 출처"라는 전제로, 결정적(비-LLM) 스크립트 2단계를 돈다.
1. **상태 점검**: `check-links.mjs` — 404/410(죽음) 판정. 301/302/303은 자동 추적되므로 죽음 아님
2. **본문 관련성**: `check-source.mjs` — 본문에 트렌드 핵심어가 몇 종 나오는지 세어 자동 판정
   - 핵심어 2종+ → `정확✅`
   - 1종만 → `약함⚠️` (사람이 직접 본문 확인)
   - 0종 → `무관❌` (딴 주제·홈·목록 페이지)
   - 본문 80자 미만(추출 실패/봇차단) → `차단🚫` (보류, 단정 금지)
- 죽거나 무관한 출처는 **검색결과에 실제로 나온 URL로만 교체**(추측·조립 금지 — 404의 주범)

### 6-3. 최종 게이트 — `auto-build.mjs` (게시 직전, 결정적 스크립트)
LLM이 아니라 코드가 마지막 문을 지킨다.
1. 중복(id/title 기존재) 제거
2. 각 항목의 출처를 **다시** 상태+관련성 검증 → 통과 출처가 1개 이상인 항목만 채택
3. 채택 항목만 `trends.json`에 반영, `images`가 비어 있으면 통과 출처의 `og:image`를 자동 주입
4. `shops`가 있으면 각 URL 생존성 재검증(죽은 링크만 제거, 이름·평판은 유지)
5. `trends.js` 재생성 → git commit & push → Vercel 자동 배포
6. 검증 통과 출처가 0개인 항목은 **자동 보류**(사이트에 안 올라감, 사람에게 보고)

> **핵심 원칙**: 봇(LLM)은 `.pipeline/curate.json`까지만 쓴다. `trends.json` 병합과 git은 전부 `auto-build.mjs`가 전담 — LLM이 직접 JSON이나 git을 만지지 않는다.

---

## 7. 카테고리별 수집 — 요일 로테이션 & 실제 검색 프롬프트

담당: `hangeut-collect`(가벼운 원자료 수집) / `hangeut-run`(온디맨드 원샷 오케스트레이터).
**전 분야 몰아치기 금지** — 무료 쿼터가 터짐. 오늘 요일에 배정된 한 분야만 깊게.

| 요일 | 분야 | 실제 검색 쿼리 예시 |
|---|---|---|
| 월 | 디저트 | `"요즘 유행 디저트 2026"`, `"오픈런 디저트"`, `"인스타 디저트 맛집"` |
| 화 | 식당·맛집 | `"성수 신상 카페 추천"` 등 지역+신상 조합 |
| 수 | 카페·핫플 | `"성수 신상 카페"` |
| 목 | 신조어·밈 | `"2026 신조어 유행어"` (목요일은 5~10개씩 넓게 모아 사전을 채움) |
| 금 | 노래·챌린지 | `"요즘 유행 노래 챌린지 2026"` |
| 토 | 패션 | `"2026 봄여름 유행 패션 아이템"`, `"요즘 인스타 패션 트렌드 2026"` |
| 일 | AI 이미지 프롬프트 | `"요즘 유행 챗GPT 이미지 프롬프트"` |

검색 도구는 항상:
```
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 "…/ddgs.exe" text -k "한국어 검색어" -m 8
```
(Windows cp949 인코딩 크래시로 한글 키워드가 0으로 잡히는 걸 막기 위해 `PYTHONIOENCODING=utf-8` 강제 — 안 하면 멀쩡한 결과도 "무관"으로 오판)

**최신 한정어를 반드시 붙인다**: "이번 주" · "요즘 가장 핫한" · "2026년 6월" · "역대급 화제" · "방금 뜬". 특히 Threads의 최근(1~2개월) 게시물에서 "요즘 유행하는 ○○" 형태가 잘 나온다고 명시돼 있음.

**중복 방지**: 후보를 확정하기 전 오늘 분야의 기존 항목을 직접 조회한다.
```js
node -e "const d=require('trends.json'); d.trends.filter(t=>t.cat==='오늘분야명').forEach(t=>console.log(t.collectedAt, t.title, JSON.stringify(t.tags)))"
```
(제목이 달라도 같은 소재면 겹친 것으로 간주 — 예: '미니어처 디오라마' vs '미니어처 룸')

**소스 우선순위**: ① ddgs 웹검색(SNS·커뮤니티·블로그) → ② 유튜브 공식 API(조회수·최근성) → ③ ddgs extract(본문 확인) → ④ 뉴스(보조).
인스타/틱톡/페북 **직접 스크래핑은 ToS 위반이라 금지** — 공개 검색결과·유튜브 API만 사용.

### AI 프롬프트 카테고리 — 특수 처리
- 지브리풍 같은 옛 프롬프트 대신, **이번 달 SNS에서 도는 새 프롬프트**(예: 하찮은/낙서풍, 손글씨, 피규어화)를 찾음
- `prompt` 필드에 실제 프롬프트 텍스트를 **20줄 이상**, 원문 그대로 담음
- 작성 시 섹션 구조를 갖춘다: `[변환목표] [스타일] [조명] [카메라] [피해야 할 것] [비율]`
- 검증(auto-build)이 "제목에 핵심어 없으면 무관"으로 판정하기 쉬우므로, 제목·출처명에 **시각적으로 구체적인 키워드**를 넣어야 통과율이 오른다(예: "네이버 블로그" → "네이버 블로그(유리꽃 프롬프트)")

### 신조어 카테고리 — 특수 처리
- `def`(한 줄 뜻, ⚠️단어 자체는 빼고 서술 — 사전 힌트용), `pureKorean`(예쁜 우리말 대체어), `example`(사용 예문)
- 한물간 옛 단어는 `"fresh": false`(또는 stage에 "끝물") → 퀴즈 자동 제외, 사전엔 '지난 유행어'로 보관
- 브랜드 안전: 외모·지역·인종 비하성 단어는 화제여도 사전에 넣지 않음

---

## 8. 전체 스킬 지도 (`hermes/skills/hangeut/*`)

| 스킬 | 역할 | 입력 → 출력 |
|---|---|---|
| `hangeut-collect` | 오늘 분야 원자료 수집(점수 없음) | — → `collect.json` |
| `hangeut-review` | ad/trust/sat 채점 + 근거(reasons) | `collect.json` → `review.json` |
| `hangeut-verify` | 최신성 재검증(게이트) | `review.json` → `verified.json` |
| `hangeut-source` | 출처 생존·관련성 검증(게이트) | curate 대상 파일 검증 |
| `hangeut-shops` | "어디서 사 먹지?" 맛집 목록 조사 | curate 항목에 `shops` 채움 |
| `hangeut-write` | 상세페이지 블로그형 본문(`article`) 작성 | curate 항목에 `article` 채움 |
| `hangeut-curate` | 스키마 변환 + 중복제거 + 디스코드 초안 | `review.json` → `curate.json` |
| `hangeut-build` | `auto-build.mjs` 호출 — 최종 게이트+반영+배포 | `curate.json` → `trends.json`+git push |
| `hangeut-run` | 위 전체를 한 세션에 온디맨드로 오케스트레이션 | "한끗 실행" 명령 진입점 |
| `hangeut-pulse` | 창업 트렌드 레이더(B2B축), 주 1회 요일 로테이션 | — → `pulse.json daily[]` |
| `hangeut-learn` | 사용자 피드백·정정을 `learnings.md`에 기록 | 피드백 → `learnings.md` |
| `hangeut-design` | 새 카테고리/카드 디자인 제안 | — |
| `hangeut-bgm` | 쇼츠·릴스 유행 배경음악 큐레이션 | — |

---

## 9. 데이터 스키마 — 자주 틀리는 형식 (실수 방지용)

- `src`는 **배열의 배열**: `[["출처명","url"], ...]` — ❌ `[{"name":..,"url":..}]` 객체 배열 금지
- `recs`도 **배열의 배열**: `[["항목","◎",1], ...]` — ❌ 문자열 금지
- `analyzedAt`은 `"YYYY-MM-DD"` — ❌ ISO 타임스탬프(`...T00:00:00Z`) 금지
- `cat`은 정규 7분류만: 디저트 · 맛집 · 카페·핫플 · 신조어 · 노래·챌린지 · 패션 · AI 프롬프트 (세분 카테고리 신설 금지 — 지역·세부는 `tags`로)
- 모든 node 명령은 **절대경로**로만 실행 (봇 CWD가 `C:\Users\admin`이라 상대경로는 100% 깨짐)