# 한끗 — 학습 노트 (고구미봇이 쌓는 교훈)

분석할 때마다 **먼저 읽고** 적용. 새로 배운 건 맨 아래에 `- [날짜] (분류) 교훈` 한 줄로 추가.
분류: 최신성 / 광고신호 / 우리말 / 소스 / 형식 / 분야

## 최신성 (지금 진짜 뜨는가)
- [2026-06-05] (최신성) 지브리풍 AI 변환은 2025년 상반기 유행 — 2026.6 현재 **한물감**. '신상'으로 넣지 말 것.
- [2026-06-05] (최신성) 약과·개성주악도 2023-24 피크 후 스테디셀러 — '지금 뜨는 트렌드' 아님.
- [2026-06-05] (최신성) 상하이 버터떡 = X발 '억지 유행' 논란 + 찹쌀도너츠와 유사. 피크→하락.
- [2026-06-05] (최신성) 지금 진짜 AI 프롬프트 유행은 **하찮은/낙서풍(scribble)** — 챗GPT 공식 템플릿 탑재. 잘 그리기 정반대 결.
- [2026-06-05] (최신성) **신조어도 빨리 늙는다.** 이왜진은 2024년산('2024 신조어 모음'으로 검색됨) → 2026.6엔 이미 2년 된 말, '신상' 아님(사용자 지적). 출처에 "2024 신조어"라 적혀 있으면 한물 의심. **2026 상반기 진짜 신조어**: 난리자베스(난리+엘리자베스=갑분 대혼란), 중지정(중요한 건 지치지 않는 정신, 중꺾마 후속), 완내뉴(완전 내 뉴런=취향저격). → 이왜진을 난리자베스로 교체.
- [2026-07-10] (최신성) 약과 디저트, 제로 슈거, 프리미엄 김밥, 퓨전 한식 주점, 대체육 요리는 2026년 7월 기준 '새로운 트렌드'라기보다는 이미 자리잡은 '카테고리'에 가까움. 더 구체적이고 막 뜨기 시작한 아이템을 발굴해야 함.

## 광고/신뢰 신호
- [2026-06-05] (광고신호) **유사 계정끼리 상호 댓글/품앗이(인게이지먼트 팟)** = 광고·협찬 강한 신호 (사용자 제보). 댓글 단 계정들이 비슷·반복 등장하면 의심 ↑.
- [2026-06-05] (광고신호) 후기 신뢰도(믿을 만한가) ≠ 만족도(좋은가). 신뢰도 높아도 내용이 '별로'면 neg (예: 두바이 초콜릿 편의점판).

## 소스·도구
- [2026-06-05] (소스) 인스타/틱톡/페북 **직접 접근 불가**(ToS·차단). ddgs 검색 공개글 + 유튜브 API(조회수·댓글)만.
- [2026-06-05] (소스) Threads에서 "요즘 유행하는 ○○" 형태 글이 최신 트렌드 잘 잡힘.
- [2026-06-05] (소스) 브라우저(Chrome) 안 됨. ddgs.exe / youtube_search.py 만.
- [2026-06-05] (소스) **출처 URL은 검색결과에 실제로 나온 것만** 쓸 것. 긴 URL을 짧게 줄이거나 추측해서 만들면 404 난다(실제로 보그·W코리아 가짜URL이 404였음). `node scripts/check-links.mjs`로 점검.
- [2026-06-05] (소스) **200이어도 본문이 그 트렌드를 안 다루면 가짜 출처.** `ddgs extract -u URL -f text`로 본문에 핵심어가 실제 나오는지 확인(hangeut-source 에이전트). 살아만 있고 무관한 링크 = 신뢰 깎임.
- [2026-06-05] (소스) **본문 관련성 도구: `node scripts/check-source.mjs [파일] [--only=id,id]`** — 각 src 본문을 받아 트렌드 키워드 수로 정확✅/약함⚠️/무관❌/차단🚫 자동 판정(LLM 안 씀). check-links(상태)의 짝.
- [2026-06-05] (소스/함정) **ddgs를 그냥 부르면 Windows cp949로 출력하다 본문에 한자·키릴·악센트가 나오면 크래시→본문이 잘려** 한글 키워드가 0으로 보인다(보그·문화일보가 거짓 '무관'으로 찍혔던 원인). `PYTHONIOENCODING=utf-8`/`PYTHONUTF8=1` 강제 필수. check-source.mjs는 자동 처리.
- [2026-06-05] (소스) **다이닝코드 profile.php·인스타그램 페이지는 JS앱/로그인벽이라 ddgs가 본문을 못 읽어 '무관/차단'으로 뜬다**(들쭉날쭉). 출처는 tistory·trip.com·티스토리계열·언론기사처럼 **본문이 실제로 읽히는 링크**로 쓸 것. (누데이크·멜로워·이스트베이글 출처를 블로그/기사로 교체함.)
- [2026-07-10] (소스/검증) `auto-build.mjs`의 출처 검증은 제목에 트렌드 핵심어가 명확히 포함될 것을 요구함. '식신', '뉴스핌', '블루리본서베이' 같은 목록형 아티클이나 "용산 맛집" 같은 일반적인 제목의 블로그 글은 '무관'으로 처리되어 보류될 확률이 매우 높음. **출처는 반드시 제목에 메뉴/가게 이름이 명시된 상세 리뷰나 기사를 사용해야 함.**

## 운영·파이프라인
- [2026-06-11] (운영) Hermes 기본 모델(gemini-2.5-flash, 구글 무료 OAuth)은 21시대에 구글측 용량 429가 잦음 — 6/10은 재시도로 살았고 6/11은 6회 재시도 전부 실패해 크론이 죽음. `fallback_providers` 폴백을 시험했으나 **둘 다 실효성 없음 확인**: OpenRouter 보유 키 3개는 크레딧 $0 무료 계정(유료 모델 402/403, 1번 키는 일일 한도 초과), Anthropic OAuth는 서드파티 앱이 extra usage 크레딧 필요(400). → **Gemini 단독 운용으로 확정**, OpenRouter 키는 hermes에서 해지(.env 주석+auth 풀 삭제, 백업 auth.json.bak.20260611_openrouter). 429로 죽은 날은 `hermes cron run 9ddacd750b48`로 수동 재실행.
- [2026-06-11] (운영) **Gemini 무료 티어는 모델별 용량 풀이 분리**: flash 일일 250회 vs **flash-lite 1000회**. 21~23시에 2.5-flash가 내내 429일 때도 flash-lite 계열(2.5-flash-lite, 3.1-flash-lite-preview)은 즉답함. → 같은 Gemini OAuth 안에서만 폴백 체인 구성, 프리뷰 모델이 은퇴(404)해도 폴백이 자동 흡수. (참고: 크론도 폴백을 탐 — 공식 문서의 \"크론 미지원\"은 낡은 내용, scheduler.py가 config를 실행 시점마다 읽음.)
- [2026-06-11] (운영) **lite 모델은 기본 모델로 부적합**: `gemini-3.1-flash-lite-preview`를 기본으로 쓴 실행(23:08)은 검색을 거의 안 하고 learnings.md에 적힌 신조어 예시(난리자베스·중지정)를 '수집'이라고 재활용해 게시 0건(부실). 기본을 **`gemini-3-flash-preview`**(flash급)로 올린 실행(23:32)은 제대로 리서치해 신선한 신조어 2건(아우라 파밍·시뮤러브) 게시+배포 완주. → 기본=flash급 품질(`gemini-3-flash-preview`). 이후 사용자 결정으로 **폴백 없이 기본 모델 단독 운용**(`fallback_providers: []`) — 429 지속·프리뷰 은퇴(404) 날은 크론 실패를 감수하고 수동 재실행.
- [2026-06-11] (형식) 트렌드 카테고리는 정규 7분류(디저트, 맛집, 카페·핫플, 신조어, 노래·챌린지, 패션, AI 프롬프트) 중 하나만 정확히 사용해야 함. '신조어·밈' 같은 복합 분류는 스키마 오류 발생.
- [2026-06-14] (소스/최신성) AI 프롬프트 트렌드 수집 시 '유리 꽃', '미니어처 디오라마'처럼 시각적으로 독특한 키워드가 SNS 반응이 좋고 출처 검증(제목 핵심어 일치)도 수월함. 출처 제목에 핵심어가 부족하면 자동 보류되므로 제목이 구체적인 링크 확보가 중요.
- [2026-06-14] (형식) AI 프롬프트는 20줄 이상의 상세 섹션(목표/스타일/조명/카메라/피해야할것) 구조를 유지해야 사용자 만족도가 높음.
- [2026-06-14] (운영) ddgs.exe가 없거나 broken일 땐 uv pip install ddgs로 즉시 복구 가능.
- [2026-06-15] (최신성) 2026년 6월 디저트 트렌드: 얼먹젤리(식감), 라바삭(이란 전통), 피스타치오 성지(성수)가 화제. 특히 얼먹젤리는 여름 시즌 특수로 매출 급증.
- [2026-06-15] (소스) 라바삭 온라인몰(세븐스타몰) 링크는 404가 잦으므로 오프라인/편의점 출처 우선 확보 필요.

- [2026-08-28] (운영/중복) `auto-build.mjs`가 '에이티즈 BAD 챌린지'를 중복으로 판정. 후보 선정 전 기존 데이터(`trends.json`)를 확인하는 절차를 철저히 지키지 않아 발생한 실수. `python -c`나 `search_files`를 이용한 사전 중복 체크를 반드시 수행해야 함.
- [2026-08-27] (분야/최신성) ‘신조어’ 직접 검색(디시, 더쿠, 네이트판)이 실패할 때, ‘밈’으로 주제를 넓히고 유튜브·뉴스 검색으로 전환하는 피벗 전략이 유효. ‘삐에로 세계관’ 밈을 이 방식으로 발굴함.
- [2026-08-27] (소스) `web_extract`가 막혔을 때, 검색 결과 제목에 나온 구체적인 밈 이름('대박삐', '삐바삐')을 다시 검색해 맥락과 추가 출처를 확보하는 간접 분석 방식이 효과적이었음.
- [2026-08-26] (운영/중복) `search_files`로 기존 항목을 확인했음에도, 그 결과를 주의 깊게 보지 않아 이미 존재하는 '카페 폭포'를 중복 수집하는 실수를 함. 후보 선정 전, 기존 데이터 목록을 한 번 더 확인하여 중복 작업을 피해야 함.


- [2026-06-05] (형식) 신조어엔 **예쁜 우리말 대체어(pureKorean)**, AI 프롬프트엔 **실제 명령어(prompt)** 필드 포함.
- [2026-06-05] (형식) **AI 프롬프트는 짧으면 안 됨 — Threads처럼 길고 디테일하게(20줄+).** 섹션 구조로: 변환목표/[스타일]/[조명]/[카메라·렌즈]/[배경]/[색보정]/[피해야 할 것]/비율. 얼굴 유지 같은 보존 규칙도 명시. (사용자 제보: Threads 정리 프롬프트가 훨씬 김.) 상세페이지 prompt-box는 줄바꿈 보존(pre-wrap)+복사 버튼.
- [2026-06-05] (형식) 상세페이지 본문은 **블로그형 `article` 블록**(`[[\"h\",..],[\"p\",..],[\"q\",..],[\"ul\",[..]]]`)으로 풍부하게. 문단 2~3문장·소제목으로 끊기(hangeut-write 스킬).
- [2026-06-05] (분야) 디저트에 치우치지 말 것. F&B(디저트·식당·카페) + 컬처(신조어·노래·패션·AI프롬프트) 골고루.
- [2026-07-11] (소스/검증) `auto-build.mjs`는 출처 URL의 생존(404) 여부뿐만 아니라, **제목에 트렌드 핵심어가 명확히 포함되어 있는지도 검증**함. '보그 코리아'처럼 일반적인 패션 기사 제목은 '무관'으로, 'W컨셉'처럼 링크가 깨진(404) 경우는 '죽음'으로 자동 필터링됨. 가게(shops) URL도 마찬가지로 검증 후 죽은 링크는 자동 제거. 출처 선정 시 제목과 링크 활성 상태를 더욱 신경 써야 함.

- [2026-07-16] (소스/검증) auto-build.mjs의 출처 검증은 제목에 트렌드 핵심어가 명확히 포함되어야 통과함. '마이스토리', '네이버프리미엄콘텐츠'의 '2026 상반기 신조어 총정리' 같은 목록형 제목은 '와호', '도파가키' 같은 특정 신조어를 직접 언급하지 않아 '무관'으로 자동 보류됨. 출처 선정 시, 다수 트렌드를 나열하는 글보다 특정 트렌드를 깊이 다루는 글이 검증에 유리.
- [2026-07-18] (소스) 자동 빌드 중 'Marie Claire Korea' 출처가 404(죽음)로 확인됨. 게시 전 링크 활성 상태 점검이 필수적임을 재확인.
- [2026-07-23] (소스/검증) '2026 신조어 총정리' 같은 목록형/모음글은 '와호', '도파가키' 같은 특정 신조어를 제목에 포함하지 않아 '무관'으로 자동 보류됨. 출처는 특정 트렌드를 깊이 다루는 상세 분석 글이 자동 검증 통과에 훨씬 유리.
- [2026-07-22] (분야/최신성) '서울 신상 카페' 같은 키워드로 검색 시, 개별 블로그 포스트보다 여러 곳을 모아 소개하는 인스타그램 포스트가 더 효율적인 트렌드 발굴 창구가 될 수 있음. 여기서 '경춘선 기차 카페'처럼 독특하고 구체적인 컨셉의 장소를 발굴하여 깊게 파고드는 전략이 유효했음.
- [2026-07-22] (분야/최신성) '요즘 카페 트렌드' 검색 시, 개별 핫플보다 업계 전반의 동향 기사(경험 소비, 업종 경계 붕괴)에서 더 강력한 트렌드를 발견할 수 있었음. 이를 '카운터컬처'라는 구체적인 컨셉으로 재정의하는 분석 방식이 유효. 막연한 키워드(신상, 이색)보다 거시적 흐름을 먼저 파악하는 것이 중요.
- [2026-07-18] (소스/검증) 'GQ Korea' 출처가 제목에 '로퍼'라는 핵심어가 없어 '무관'으로 자동 보류됨. 출처 제목에 트렌드 핵심어가 명확히 포함되어야 자동 검증 통과에 유리함.
- [2026-07-25] (블로그/발행) **티스토리 Open API는 2024-02 종료 — 공식 글쓰기 API가 없다.** 남은 길은 Playwright뿐. 단 **카카오 로그인은 자동화하지 말 것**(셀렉터·2FA·기기인증이 전부 깨질 지점). 전용 프로필(`backend/.tistory-profile`)에 사람이 1회 로그인해 세션을 재사용하면 그 전부를 우회한다. 본문은 타이핑 대신 `tinymce.activeEditor.setContent()`로 한 번에 주입해야 양식이 안 깨진다.
- [2026-07-25] (블로그/이미지) **trends.json의 `images` URL은 블로그 본문에 핫링크로 쓸 수 없다.** 실측: 나무위키(i.namu.wiki) 403, 한국경제(img.hankyung.com)는 curl 200인데 브라우저에선 영영 로딩 중(`complete:false`). 내려받아 티스토리에 업로드해야 안 깨지고 썸네일도 잡힌다. 또한 103건 중 **25건은 images가 아예 없음**(대부분 신조어) — 커버 없는 글이 생기는 원인.
- [2026-07-25] (SEO) **`trend.html`은 103건 전부 `<title>`이 "한끗 — 트렌드 분석"으로 동일**하고 본문은 `trends.js`에서 클라이언트 렌더. sitemap.xml·robots.txt도 없다. 개별 트렌드가 검색에 잡히지 않는 구조 — 검색 유입을 원하면 트렌드별 정적 페이지(고유 title·description) 생성이 선행돼야 한다.
- [2026-07-25] (블로그/환각) **LLM 단독 생성은 모르는 영역에서 전량 환각이다.** 실측: `jpop-hidden`(2025년 이후 무명곡) 후보 8곡 **전부 존재하지 않는 곡**. 반면 `retro-revival`(80년대 시티팝)은 8곡 중 7곡이 실존(角松敏生·松任谷由実 등) — LLM 이 아는 영역이라. 처방은 **검색-우선(retrieval)**: 유튜브에서 실존 곡을 먼저 모아 LLM 은 고르기만 하게 한다. 같은 병이 `ai-trend`에도 나타났다(2026년 AI 동향 출처를 지어냄) → 뉴스 계열도 웹검색 근거가 선행돼야 한다.
- [2026-07-25] (블로그/저작권) **댓글 원문을 LLM 에 주면 반드시 가사가 새어 나온다.** 유튜브 댓글엔 가사가 통째로 달리고, 프롬프트로 세 번 금지해도 매번 번역 가사를 따옴표로 인용했다. 프롬프트가 아니라 **구조로 막아야 한다** — 댓글을 먼저 구조화 요약(mood/whenListened/praised)으로 바꾸고 원문은 삭제한 뒤 본문 작성기에 넘긴다. 안 주면 인용할 방법이 없다.
- [2026-07-25] (블로그/설계) **LLM 이 정직하게 쓸 수 없는 항목을 요구하면 지어낸다.** "소리 묘사"를 요구하니 안 들어본 곡의 편성을 추측했고, "어떤 이야기인가"를 요구하니 가사를 인용했다. 발굴 모드에선 근거 있는 것(조회수·구독자·hiddenRatio·댓글 요약)만 요구하도록 `mustInclude` 를 바꿔야 한다.
- [2026-07-25] (블로그/지표) '숨은 명곡'을 조회수만으로 거르면 안 된다. **hiddenRatio = 조회수 ÷ 채널 구독자**가 맞는 지표 — 낮을수록 그 아티스트 규모에 비해 안 들린 곡이다. 대형 아티스트의 묻힌 곡을 하드 필터로 자르지 말 것(그게 진짜 발굴). 유튜브 검색 결과의 **약 40%는 메들리·모음집**이라 별도 정규식 필터가 필수.
- [2026-07-25] (소스) 자동 빌드 중 '코오리아' 출처가 404(죽음)로 확인되어 자동 제외됨. 게시 전 `check-links.mjs`의 중요성을 재확인.
- [2026-07-25] (블로그/할당량) **유튜브 검색은 1회 100유닛, 무료 일일 10,000유닛 = 하루 100회.** 검색어 12개짜리 발굴을 테스트 몇 번 돌리자 429 로 소진됐다. 검색어는 날짜 회전으로 5개씩만 쓰고 결과는 하루 캐시한다(`--fresh` 로 무시). 리셋은 태평양시 자정.
- [2026-07-25] (블로그/LLM) Gemini 2.5 는 **추론 토큰도 maxOutputTokens 에 포함**된다. 8192 로 두면 JSON 이 중간에 잘려 "파싱 실패"로 보인다 — 32768 로 올리고 `finishReason=MAX_TOKENS` 를 따로 잡아 보고해야 원인이 보인다. 또 채점 프롬프트에 집필 지시(구조 템플릿)를 섞으면 모델이 채점 대신 글을 다시 쓴다 — 채점 지시를 앞뒤로 두고 본문은 울타리로 감쌀 것.
- [2026-07-25] (블로그/근거) **뉴스·동향 글도 검색-우선이어야 한다.** ddgs 로 실제 기사를 찾아 **본문까지 읽어(extract)** 넘기면 통과한다(실측: 근거 없이 32/40 미달·가짜 출처 → 근거 6건 주입 후 32/40 통과, 링크 5개 전부 실존 200). `lib/websearch.mjs` 의 `gatherSources()` 가 담당 — 본문 300자 미만이면 버려서 "출처는 있는데 내용은 모르는" 상태를 막는다.
- [2026-07-25] (블로그/게이트) **LLM 검수는 누락에 약하다.** "URL 지어내지 마라" 압박을 받자 모델이 링크를 전부 빼버렸는데 검수는 적합성 9/10 으로 통과시켰다. 링크 유무·개수는 의견이 아니라 사실이므로 **문자열로 세는 기계 검사**(`hardChecks`)를 따로 둔다. 허용 URL 집합에 없는 링크 = 지어낸 링크로 결정적 판정 가능.
- [2026-07-25] (블로그/모순) **지시가 서로 모순되면 모델은 지어내서 채운다.** 프로파일 `ctaType`이 "관련글 내부링크 2개"를 요구하는데 그 카테고리에 글이 0개면 example.com·가짜 블로그 URL 이 나온다. 링크할 글이 없을 땐 `ctaType` 자체를 "링크 없이 마무리"로 바꿔서 넘겨야 한다.
- [2026-07-26] (블로그/검증) **`verifySong`이 "泰葉 (Yasuha)"처럼 로마자 병기된 아티스트명을 괄호째로 매칭해 사실상 항상 실패했다.** 괄호 뒤 로마자까지 통째로 `includes()` 대상 문자열에 넣으니 실제 영상 제목("Yasuha Official Lyric Video")과 안 맞아, 레이블 공식 업로드(조회수 3천만)를 놓치고 비공식 업로드(19만)만 잡았다. 수정: `artist.split('(')[0].trim()`으로 괄호 앞부분만 잘라 매칭(`lib/youtube.mjs`). 부분 일치 자체는 문제가 아니었다 — 매칭 대상 문자열이 잘못 조립된 게 원인.
- [2026-07-26] (블로그/형식) **마크다운 파싱 — URL이 괄호 안에 있을 때 제대로 추출되지 않으면 임베드/링크가 깨진다.** `lib/md.mjs` 정규식 개선. 추가로 `[YT: id | caption]` 임베드 및 `---` 구분선 문법 지원.
- [2026-07-26] (블로그/발행) **티스토리 세션 만료 시 `blog-publish.mjs --login`으로 재인증 필요.** 카카오 로그인 자동화 불가(2FA·기기인증·CAPTCHA 불확정적). `backend/.tistory-profile` 프로필 쿠키 갱신 또는 수동 로그인으로만 해결.
- [2026-07-26] (블로그/발행) **티스토리 발행 직후 URL 기록 — 관리 페이지 URL이 모든 글에 같으면 `blog-publish.mjs`가 RSS 피드를 파싱해 실제 퍼머링크를 확정.** 관리 페이지 URL이 아니라 RSS 최신 글 `<link>`를 신원으로 삼아야 블로그 목록이 제대로 누적된다.
- [2026-07-25] (블로그/조사품질) **ddgs 와 WebSearch 는 물어오는 자료의 급이 다르다.** 같은 "2026 상반기 LLM 판도" 주제로 ddgs 는 개인 블로그·러시아어 CORS 튜토리얼만 물어와 **숫자가 하나도 없는 글**이 나왔고, WebSearch 는 GPT-5.4 GDPVal 83%·Claude Opus 4.6 SWE-bench 81.4%·Gemini 3.1 Pro GPQA 94.3% 같은 실제 수치와 llm-stats·aireleasetracker 리더보드를 가져왔다. 중요한 글은 Claude Code 로 조사해 `backend/blog/research/<slug>.json` 에 넣고 `post-build.mjs --research=` 로 주입한다.
- [2026-07-25] (블로그/에이전트) **한 모델이 조사·집필·검수를 다 하면 자기가 지어낸 걸 자기가 검수한다.** 역할을 나누고 뒷 단계가 앞 단계를 의심하게 만들어야 한다: `blog-researcher`(1차 자료만) → `blog-curator`(사실/추정 분리·못 다룰 것 확정) → `blog-writer`(근거 밖 금지) → `blog-seo`(메타만) → `blog-factchecker`(문장 단위 대조, opus). 산출물은 전부 파일로 넘겨 중간부터 재실행·사람 확인이 되게 한다. `.claude/skills/blog-pipeline` 이 순서를 관장.
- [2026-07-27] (블로그/자동화) **n8n 워크플로 JSON 은 LLM 이 지어내면 100% 깨진다** — 노드 `type` 문자열·`typeVersion`·파라미터명이 정확해야 임포트가 된다. 검증 절차를 확립했다: (1) n8n **공식 템플릿 API**(`api.n8n.io/api/templates/workflows/<id>`)로 실제 동작하는 워크플로를 받아 노드 정의를 베낀다 (2) `npm i n8n` 으로 실제 설치해 `node_modules/n8n-nodes-base/package.json` 의 `n8n.nodes` 목록과 노드 클래스의 `nodeVersions` 로 타입·버전을 대조한다 (3) `n8n import:workflow` → `export:workflow` 왕복 후 원본과 diff 해 무손실을 확인한다. 파라미터명이 헷갈리면 설치본의 `dist/nodes/<Node>/**/Description.js` 를 grep 하는 게 문서보다 정확하다. ⚠️ CLI 임포트는 최상위 `id` 가 없으면 `SQLITE_CONSTRAINT` 로 실패한다(UI 붙여넣기는 자동 생성) — 배포용 JSON 엔 `id`/`versionId` 를 넣어라.
- [2026-07-27] (블로그/조립) `blog-assemble.mjs` 의 '지어낸 URL' 검사가 **코드블록 안 예시 URL**(에러 메시지의 `localhost:8080`, 샘플 코드의 주소)까지 잡아 web-error·automation-idea 처럼 코드가 나오는 글을 전부 막았다. 검사 전에 ```` ``` ```` 블록을 제거하도록 고침. 인용 링크만 검사 대상이다.
- [2026-08-25] (소스/검증) `auto-build.mjs`의 출처 자동 검증은 제목에 구체적인 트렌드 키워드가 포함되어야 통과함. '오하루수육국밥' 수집 중, '망원동 신상맛집'처럼 일반적인 제목의 블로그 후기는 '무관'으로 자동 제외됨. 출처 선정 시, 제목의 구체성이 중요함을 재확인.
- [2026-08-24] (소스/검증) `auto-build.mjs`의 출처 자동 검증은 제목에 구체적인 트렌드 키워드가 포함되어야 통과함. '무화과 디저트' 수집 중, 일반적인 제목의 Threads 링크가 '무관'으로 자동 제외됨. 출처 선정 시, 제목의 구체성이 중요함을 재확인.
- [2026-08-19] (분야/최신성) '연남동 신상 카페'처럼 특정 지역의 '신상'을 직접 검색하는 방식은 후기 부족으로 분석이 어려운 경우가 많음. '이색 카페'처럼 더 넓은 키워드로 목록형 게시물을 찾고, 그 안에서 독특한 컨셉(예: 카페폭포)을 발굴하는 '미시적 접근'이 더 효과적임.
- [2026-08-19] (소스/검증) `auto-build.mjs`의 출처 검증은 제목에 '카페폭포' 같은 명확한 고유명사가 포함되어야 통과율이 높음. '아이랑 가볼만한 곳' 같은 일반적인 제목은 '무관'으로 분류될 수 있음을 재확인.
- [2026-07-29] (분야/최신성) 신규 오픈한 장소(폼폼푸린 카페)는 후기가 없어 '신뢰분석' 자체가 불가능함. 화제성이 있더라도 검증 가능한 후기가 쌓일 때까지는 트렌드 분석을 보류하는 것이 적절함.
- [2026-07-29] (분야/최신성) '이번 주 새로 생긴'과 같은 매우 구체적인 발견형 키워드는 결과가 없는 경우가 많음. '전략적 피벗'을 통해 '이색 카페' 같은 목록형/모음글에서 구체적인 타겟을 발굴하는 '미시적 접근'이 더 효과적이었음.
- [2026-07-28] (소스/검증) `auto-build.mjs`의 가게(shops) 링크 검증 기능이 실제로 동작하는 것을 확인. '다운타우너', '잠수교집' 등 연결할 수 없는 URL 3개를 자동으로 제거해줌. 봇은 가게 목록 자체를 모으는 데 집중하고, 링크 생존성 검증은 빌드 스크립트에 맡기는 것이 효율적.
- [2026-07-27] (블로그/조사) **조사 에이전트가 인용문과 날짜를 지어낼 수 있다** — Y2K 플래시 필터 글에서 \"SocialBee 가 '올해 가장 바이럴한 이펙트'라고 5일 전 보도\"라고 보고했으나, 원문을 직접 WebFetch 해보니 그 문구 자체가 없고 게시일도 7주 전이었다. **트렌드 근거로 쓸 인용문·날짜는 코디네이터가 원문을 직접 열어 대조한 뒤에만 본문에 넣는다.**
- [2026-07-28] (블로그/말투) **\"프로파일의 tone.formality 를 따른다\" 같은 간접 지시는 그냥 무시된다.** 실측: 프로파일이 해요체인데 합니다체 31 / 해요체 0으로 나갔다(집필 프롬프트에도 \"해요체\"를 명시했는데도). 팩트체커는 사실 검증에 집중하느라 이런 형식 위반을 놓치기 쉽다. → `blog-assemble.mjs` 가 `니다[.!?]` vs `(어요|에요|예요|아요)[.!?]` 개수를 세어 프로파일과 어긋나면 조립을 거부하도록 기계 검사를 추가했다. URL 검사와 같은 원리 — **판단이 아니라 셈으로 막는다.**
- [2026-07-28] (블로그/귀속) **한끗 자체 분석을 언론 보도로 귀속하면 안 된다.** trends.json 의 `article`·`verdict` 는 Hermes 의 분석이고 `src` 는 "이 트렌드를 다루는 기사"일 뿐이다 — auto-build 가 검증한 건 *주제 관련성*이지 *특정 문장이 그 기사에 있는가*가 아니다. "○○신문도 이렇게 짚었다"고 쓰면 확인 불가능한 인용이 된다. 매체는 참고 링크로만 걸고 화자로 세우지 말 것.
- [2026-07-28] (블로그/근거) **한 가게의 특징을 시장 신호로 부풀리는 실수가 있었다.** `shops[].note` 의 "웨이팅이 필수"(다운타우너 한 곳)를 "매장 웨이팅이 늘고 있다"는 시장 전체 신호로 썼다. 원본 근거는 배민 주문 데이터 하나뿐이었다. 근거가 하나면 하나라고 쓴다 — 두 개인 것처럼 겹쳐 쓰면 그게 과장이다.
- [2026-07-28] (블로그/중복) **한끗 상세페이지와 자기중복이 생긴다.** trends.json 의 `article` 을 근거로 쓰다 보면 문장까지 닮아, 12자 이상 n-gram 이 87개 겹친 사례가 있었다. 같은 사실을 쓰되 **구성 순서부터 바꿔라** — article 블록을 순서대로 따라가면 반드시 겹친다. 문장 단위 완전일치 검사만으론 못 잡으니 n-gram 으로 봐야 한다.
- [2026-07-28] (블로그/썸네일) **티스토리는 '대표 이미지'를 본문과 별개로 저장한다.** 본문 이미지를 새로 갈아도 목록 썸네일·og:image 는 첫 발행 때 지정된 옛 이미지를 계속 가리킨다(실측: 커버 12편을 교체했는데 본문만 바뀌고 목록은 옛 카드 그대로였다). `[##_Image|...]` 치환자 JSON 에도 대표 플래그가 없다 — 글 메타로 따로 관리된다. 해결: 에디터에서 이미지를 클릭하면 뜨는 `div.mce-represent-image-btn` 을 눌러 `.active` 를 만들어야 한다(토글이라 이미 active 면 다시 누르면 해제된다). `setRepresentative()` 로 발행·교체 양쪽에 넣었다.
- [2026-07-31] (소스/검증) `auto-build.mjs`의 출처 자동 검증 중, 'mosi mosi? 챌린지'의 나무위키 출처가 404(죽음)로 확인되어 자동으로 제외됨. 나머지 유효한 출처만으로 성공적으로 게시. 이는 자동화된 링크 활성 상태 검증 게이트의 실효성을 재확인시켜 줌.
- [2026-07-28] (블로그/썸네일) **목록 썸네일은 256x256 정사각 crop 이다.** 티스토리가 원본을 `R750x0`(750x393)으로 줄인 뒤 테마가 `object-fit:cover` 로 정사각 크롭한다 → 1200x630 카드에서 **실제로 보이는 건 가운데 약 630px 폭**뿐. 좌측정렬 카드는 제목이 양쪽 다 잘리고 브랜드·날짜·카테고리칩이 통째로 사라진다. 카드는 **중앙정렬 + 핵심정보를 가운데 620px 안에** 배치해야 한다.
- [2026-08-07] (최신성) 한번 유행이 끝난 챌린지도 새로운 스타(2억 뷰 초등학생)의 등장으로 재점화될 수 있음. '슬릭백'처럼 역주행하는 트렌드는 새로운 맥락으로 분석 가치가 있음.
- [2026-08-16] (소스/검증) '네이버 블로그', '유튜브' 등 일반적인 출처명과 'AI 애니메이션' 같은 포괄적 제목은 특정 프롬프트(예: 막그림 독설툰)와의 연관성을 입증하지 못해 '무관'으로 자동 보류됨. 출처 제목에 '2D 스티커'처럼 분석 대상의 핵심 키워드가 명확히 포함되어야 자동 검증 통과율이 높아짐.
- [2026-08-09] (운영) `auto-build.mjs` 실행 중 `non-fast-forward` git 오류 발생. 원격 저장소와의 동기화 문제로, 로컬 데이터는 갱신되었으나 자동 배포(push)는 실패. 수동 개입(pull, rebase, push) 필요.
- [2026-08-09] (분야/최신성) AI 프롬프트 수집 시, '요즘 유행' 같은 직접 키워드 검색이 실패할 경우, SNS에서 보이는 시각적 테마(예: 클레이 애니메이션)를 먼저 가설로 설정하고 역으로 유행 여부를 검증하는 방식이 유효함.
- [2026-08-06] (소스/검증) '2026 신조어 총정리' 같은 목록형/모음글은 특정 신조어(예: '야르', '영크크')를 제목에 포함하지 않아 '무관'으로 자동 보류됨. 출처는 특정 트렌드를 깊이 다루는 상세 분석 글이 자동 검증 통과에 훨씬 유리함.
- [2026-08-06] (운영) `auto-build.mjs` 실행 중 `non-fast-forward` git 오류 발생. 원격 저장소와의 동기화 문제로, 로컬 데이터는 갱신되었으나 자동 배포(push)는 실패. 수동 개입(pull, rebase, push) 필요.
- [2026-08-02] (성장전략) **네이버/티스토리 고트래픽 리서치 — 3건 조사 + 4건 교차검증 완료.**
  **써도 되는 것(CONFIRMED/다중 소스)**: 제목 앞부분 키워드 배치(네이버·구글 공통) · 숫자형
  제목은 여러 독립 대규모 분석에서 일관되게 CTR 긍정적(정확한 %는 신뢰 낮음) · 표·리스트가
  텍스트보다 스니펫 선정·체류시간에 유리 · 티스토리는 **구글 오가닉이 67~71%**(SEMrush 도메인
  전체 통계, 2026-02·06 두 스냅샷)로 압도적 — 단 **신생 블로그·카테고리는 6~16개월간 다음·
  네이버가 먼저 붙고 구글은 뒤늦게 역전**(llily.co.kr 실측: 개설 후 한동안 구글 0~5건/일 vs
  네이버 400건/일) · referral/커뮤니티형 콘텐츠는 예외(검색 자체가 주력이 아님).
  **의외로 반박된 것**: "~하는 법"/"팁" 문구를 제목에 습관적으로 붙이는 것 — HubSpot×Outbrain
  330만 헤드라인 분석에서 "how to" -49%·"tip" -59%(단 피드 노출 맥락, 오가닉 검색은 다를 수
  있어 검색의도 일치형 제목(에러 해결 등)까지 부정하는 건 아님). "2026년에도 있는" 류 애매한
  시간참조 표현도 Chartbeat 25만 헤드라인 분석에서 CTR 하락 방향.
  **완전히 근거 없음 — 절대 인용 금지**: "네이버가 AuthGR·Next N Search 로 전환했다"는 주장은
  SEO 마케팅 블로그들(retn.kr 등)끼리 서로 다른 정의로 재가공하며 도는 **환각에 가까운 조어**다.
  1차 출처(네이버 공식·DAN25 컨퍼런스 자료)·정통 IT언론 어디에도 없다. (실제 사실:
  네이버 연관검색어 서비스 2026-04-30 종료·AI 브리핑 확대 추세·"네이버 메이트" 크리에이터
  지원 프로그램(월 30만~1,000만원)은 공식 보도자료로 확인됨 — 이것만 사실로 취급할 것.)
  **네이버 C-Rank/D.I.A 는 blog.naver.com 전용 로직**이라 티스토리엔 세부 메커니즘이 적용 안
  됨(별도 웹문서/사이트 컬렉션 파이프라인, 전통 SEO에 가까움) — 이 알고리즘을 깊이 스터디하는
  건 티스토리 운영엔 시간 낭비, 대신 **네이버 서치어드바이저 등록·사이트맵 제출**(계정 필요,
  사람이 해야 함) 쪽이 실질 레버.
  "정보성(정책/지원금) 카테고리가 방문자수에 강하다"는 통념은 확인도 반증도 못 함 — 이 근거로
  새 카테고리를 만들지 말 것. 전체 리서치·검증 원문은 이 세션 트랜스크립트에 있음.
- [2026-09-01] (분야/소스) '신조어' 직접 검색이 막힐 때, '밈'으로 주제를 넓히고 '뉴스' 채널을 활용하는 피벗 전략이 유효. '거제 야호', '회사 부수기' 밈을 이 방식으로 발굴함.
- [2026-08-30] (운영/중복) `read_file`로 대용량 JSON 파일 확인 시, 잘린 부분을 인지하지 못하고 중복 항목('펀코팝 프롬프트')을 수집하는 실수 발생. `search_files`나 `python -c`를 이용해 특정 카테고리만 필터링하여 확인하는 절차를 반드시 거쳐야 중복 작업을 피할 수 있음.
- [2026-08-29] (분야/소스) 패션 트렌드 수집 시, 하퍼스 바자, 엘르, 보그 등 주요 패션 매거진의 최신(1-2개월 내) F/W 시즌 분석 기사는 신뢰도 높은 최신 정보 소스로 매우 유용함.
- [2026-08-30] (블로그/검증) 트렌드 글 CTA에서 상세페이지 "점수" 안내 금지. 광고 의심도·후기 신뢰도
  점수는 `type:"신뢰분석"`(ad/trust 값 있음)에만 있고, `type:"트렌드"`(ad·trust=null)는 trend.html이
  점수 대신 '유행 단계' 박스만 렌더한다 — 트렌드 글은 "유행 단계와 한끗 판단"으로 안내(clown-meme·
  purple 배치에서 blog-verify가 2건 반려). 또 제목에 "광고 없이" 같은 확정 단언 금지(ad=20도 추정치)
  → "광고보다 입소문에 가까운" 식으로 완화(east-bagel 반려 교정).
- [2026-09-03] (운영/중복) `read_file`로 대용량 JSON(`trends.json`) 확인 시, 일부만 보고 중복 항목('거제 야호')을 인지하지 못하는 실수 발생. `python -c` 또는 `search_files`를 이용해 특정 카테고리만 정확히 필터링해야 중복 수집을 막을 수 있음.
- [2026-09-04] (최신성) 챌린지 수집 시 유튜브 검색 결과의 날짜가 1~2년 전이면 한물간 것으로 간주. 2026년 게시물이 다수인 '붐 샤카라카'를 채택하고 '한강 고양이'(24-25년)는 폐기.
- [2026-09-04] (소스) "요즘 유행 2026" 같은 검색어가 결과를 내지 못할 때, 연도 키워드를 빼고 "요즘 유행 틱톡"처럼 일반화하여 검색 백엔드를 바꿔가며 재시도하는 전략이 유효했음.
- [2026-09-08] (소스/검증) `auto-build.mjs`의 출처 검증은 제목에 핵심어가 포함되어 있어도, 다른 수식어가 많으면 '무관'으로 오판할 수 있음. '꾸왁칼국수' 수집 시, \"을지로 주민 또간집 꾸왁칼국수...\"라는 제목의 출처가 '무관'으로 자동 보류됨. 출처 선정 시 제목이 간결하고 핵심어를 명확히 포함하는 링크를 우선해야 함.
  자리표시자라 팩트체커가 대조할 원문이 0, 사실 근거가 Gemini `trend` 필드뿐. 기술글(dev-digest·toss,
  WebSearch 조사)과 근거 밀도 10배 차. (2) 반복 결함을 프롬프트가 아니라 **영구 가드**에 박았다:
  blog-pick-trend 가 묵은 피크(collectedAt/analyzedAt 중 오래된쪽 >21일)·얇은 출처(<2)·읽을 수 없는
  소셜 URL 을 ⚠ 플래그하고 경고 없는 후보를 먼저 뽑음; blog-assemble 이 트렌드 글 CTA '점수'·builtAt≠오늘
  을 검사하고 거짓 각주("사실 확인 안 된 항목 표시")를 제거; structures/hangeut-*.md 에 소제목 변주·
  reasons=null 금지·표본 고지·묵은 피크 과거시제 규칙 추가. (3) 하루 5~7편은 양산형 — **4편 이하**로,
  얇은 단일출처 트렌드보다 조사된 기술글·표본 있는 trust 를 우선. 새 결함이 또 반복되면 배치 프롬프트가
  아니라 이 가드들에 추가할 것.
- [2026-09-05] (소스/검증) 패션 트렌드 수집 중 '2026 F/W를 관통하는 10가지 패션 트렌드' 같은 일반적인 제목의 출처('네이버매거진', 'Bini00's blog')는 '페플럼', '애니멀 프린트' 같은 특정 키워드가 없어 '무관'으로 자동 보류됨. 출처 제목에 분석하려는 트렌드의 핵심어가 명확히 포함되어야 자동 검증을 통과할 수 있음을 재확인.
- [2026-09-05] (블로그/품질) '작성 후 자체 피드백→반영'을 상설 단계로 박음: `backend/scripts/blog-selfreview.mjs`.
  집필 직후 `claude -p` 가 초안을 냉정히 자기비평하고 **실제 결함만 외과적 find/replace**로 고쳐 넣은 뒤 재조립한다
  (전면 재작성 금지 — 드리프트·새 오류 방지: LLM은 '어디를 무엇으로'만 집고, 적용은 스크립트가 결정적으로. find가
  본문에 없거나 여러 번이면 건너뜀). 표준 흐름: 조사 → 집필 → blog-selfreview → blog-verify → 발행. selfreview는
  비치명적(실패해도 발행 안 막음 — 뒤의 verify가 최종 게이트). 실측: pizza-seolgi에서 ad(낮을수록 좋음)·trust(높을수록
  좋음)를 한 눈금으로 비교한 축 오독을, verify가 ⚠로만 본 걸 selfreview가 집어냈다.
- [2026-09-06] (소스/검증) '페이퍼컷 아트' 수집 중 'Sketchto Paper Cutout Generator', 'YouMind 3D Paper-Craft Diorama Prompt' 등 일반적 제목의 출처는 '무관'으로 자동 보류됨. 출처 제목에 '페이퍼컷' 같은 핵심어가 명확히 포함되어야 자동 검증 통과 가능.
- [2026-09-06] (블로그/자동화) aztomz-blog-daily 스킬은 `claude.cmd`의 비대화형 셸 로그인 문제로 현재 작동하지 않음. `cmd.exe`를 통해 호출 시 표준 출력이 유실되어 결과를 얻을 수 없음을 재확인. Anthropic API 직접 호출 방식으로 전환 필요.
- [2026-09-11] (블로그/자동화) `aztomz-blog-daily` 크론 작업이 `claude.cmd`의 `Failed to authenticate: OAuth session expired` 오류로 인해 실패함을 재확인. 이는 스킬 문서에 명시된 알려진 문제와 일치하며, 비대화형 셸에서의 CLI 인증 한계를 보여줌. `process_manage` 도구를 통해 실패 로그를 성공적으로 확보함.
- [2026-09-05] (분야/전략) 블로그차트(blogchart.co.kr/chart/week)는 블로그(사이트) 순위를 ~32 카테고리로
  보여준다 — 상위는 인테리어·부동산·금융재테크·의학·생활정보성이 지배(트래픽은 크나 우리 도메인 아님,
  실경험·E-E-A-T 필요라 정직하게 못 씀). 공략 플랜은 backend/blog/blogchart-plan.md — 트래픽 큰 카테고리
  ∩ 우리가 실출처로 쓸 수 있는 것(맛집·디저트·패션·여행·IT/AI·미용·가전·반려·콘텐츠)으로 10편, 전부
  WebSearch 근거, 하루 4편 이하로 3일 분산, 쓰는 날 그날 조사. 인테리어·부동산·금융·의학은 제외(환각 위험).
- [2026-09-06] (블로그/품질) 09-06 배치(경량VLM·가을음식·캠핑·추석)에서 **selfreview가 반복해서 잡은 6가지 결함** —
  집필 단계에서 미리 피하고, 또 반복되면 writer 프롬프트가 아니라 selfreview 규칙·구조 템플릿 가드에 박을 것:
  (1) **발행일 종속 표현 금지** — 티스토리 발행이 수동이라 작성일≠발행일. '오늘·지금·이번 주'처럼 작성 시점에
  묶인 말은 나중 발행되면 즉시 거짓이 된다(예: 축제가 작성일에 끝나는데 '지금 가장 확실히 만날 수 있는 곳').
  절대 날짜로 고정하라. (2) **표본 얇은데 일반화 금지** — 축제 1건을 '서해안 항구마다', 단일 사례를 '자주'로
  부풀리지 말 것(조사에 1건이면 1건으로). (3) **부재를 세상 전체로 단정 금지** — '공식·언론 자료에 없다'가 아니라
  '이번 조사에서 못 찾았다'로. (4) **출처 오귀속/과잉귀속 금지** — 필자의 eli5 비유·추론을 HF 공식 블로그·기관 뉴스에
  귀속하지 말고, 수치는 실제 실린 출처(모델카드)에만 귀속(ETH 뉴스엔 없는 4조 토큰을 거기 귀속하면 왜곡).
  (5) **묵은 통계 현재형 금지** — 2024년 보도 통계를 기준연도 없이 '최근 3년간'으로 쓰면 최신 집계로 오독됨. 기준 시점 표기.
  (6) **출처 권위 격상 금지** — 언론 건강코너(머니투데이)를 '의료 매체'로 올려 의학적 권위 부여 금지. 사망 사례도 부검 전이면
  '확정 사인'처럼 쓰지 말 것. + VLM에선 '라즈베리파이 예산 근거를 정반대 결론에 붙인' **논리 역전**도 나왔다(사실은 맞아도
  인과·판정 방향이 뒤집힐 수 있으니 selfreview가 유효). verify(claude-fable-5-1)는 JSON 파싱이 가끔 실패하니 1회 재실행.
- [2026-09-08] (블로그/전략) trends.json에 ⚠없는 clean 후보가 0(51건 전부 ⚠)이고 blogchart 10편 플랜도 소진·추석/가을 seasonal 포화면,
  얇은 ⚠후보로 물량 채우지 말고 **그날 WebSearch로 겹치지 않는 시의성 4편**을 새로 조사해 발행한다(BIFF /402·가을축제 /403·가을러닝 /404·온디바이스AI /405).
  이날 실측 교훈 3가지: (1) blog-writer가 라이프/기술 글을 **해요체 아닌 문어체(~다)**로 써 오면 assemble 말투 게이트가 거부 → 해당 .md만 해요체로
  변환 후 재조립(4편 중 2편에서 발생). (2) selfreview가 '이미 끝난 축제(서울불꽃 9/5)를 미래형 추천'·'조사에 없는 마감일(5/27) 창작'·'2024 통계 출처
  오귀속'을 실제로 잡아냄 — 집필 단계에서 미리 피할 것. (3) worktree엔 티스토리 프로필이 없어 발행 불가 → 완성본 backend/out/blog/<slug>.{html,json}을
  메인 체크아웃으로 복사해 메인 스크립트로 발행(커밋·푸시되는 durable 산출물은 research/*.json뿐).
- [2026-09-09] (소스/검증) `auto-build.mjs`의 제목 검증 로직이 매우 엄격. '메이드 카페' 수집 중, \\\"홍대 '메이드카페'는 뭐하는 곳?…\\\", \\\"메이드리밍 메이드 카페 후기...\\\" 등 제목에 명확한 핵심어가 포함된 출처 3개가 전부 '무관(제목 핵심어 없음(일반어뿐))'으로 자동 보류됨. 제목이 간결하고 핵심어 외 수식어가 적은 출처를 우선하거나, 검증 스크립트의 허용오차 조정 필요성을 시사.
- [2026-09-09] (블로그/검증) **CLI(claude.cmd) OAuth 세션이 만료되면 blog-selfreview·blog-verify가 전부 죽는다** —
  nested 호출하는데 "Failed to authenticate: OAuth session expired and could not be refreshed"로 실패(그날 낮 만료 실측, 어젠 됐음).
  복구=터미널서 `claude` 재로그인. Hermes `/blog-daily` 크론도 같은 이유로 영향. **게이트가 죽으면 사람이 수동 verify**: 각 원고의
  구체 수치·고유명사를 그 편 `research/<slug>.json` 과 문자열 대조(`node -e "blob.includes(n)"`)하면 writer 창작이 즉시 드러난다 —
  이날 검색수요 4편(추석 병원/통행료/선물세트·가을야구, /410~413) **전부에서 지어낸 수치 발견·제거**: 가을야구 승-무-패 전적·아시안게임 날짜·2022 개막일,
  병원 2024 당직수(2만7천/1천8백), 선물세트 신세계 품목 세부개수(합이 총계와도 안 맞음), 통행료 매체명 오류(에일리→이데일리).
  ⚠ **assemble·말투 게이트는 창작 수치를 못 잡는다** — verify(자동이든 수동이든)가 유일한 방어선. writer는 표·수치를 그럴듯하게 지어내는 경향.
- [2026-09-14] (운영/중복) `read_file`로 대용량 JSON(`trends.json`) 확인 시, 일부만 보고 중복 항목('왁뿌소금빵')을 인지하지 못하는 실수 발생. `python -c` 또는 `search_files`를 이용해 특정 카테고리만 정확히 필터링해야 중복 수집을 막을 수 있음.
- [2026-09-14] (소스/검증) '설빙 허쉬 초코빙수' 수집 시 확보한 출처 2개가 모두 404(죽음)로 확인되어 자동 보류됨. `check-links.mjs`를 이용한 링크 활성 상태 사전 점검의 중요성을 재확인함.
- [2026-09-13] (블로그/파이프라인) `backend/blog/keywords.csv` 백로그(ai-trend·automation-idea·ai-prompt-pack)를 정식 5단계 파이프라인(researcher→curator→writer→seo→factchecker)에 처음 태운 배치. `claude -p` 여전히 OAuth 만료라 selfreview 스킵됐지만 **blog-factchecker 에이전트(WebFetch로 원문 재대조)는 정상 작동** — AI동향 글에서 "OpenAI 발표문이 가격 2.5배 인상을 밝혔다"는 문장이 실은 Artificial Analysis 기사 문장이었던 **출처 오귀속**과, 메타설명의 근거 없는 "10배" 수치를 blocker로 잡아냄. n8n 글에서는 "RSS 새 글이 여러 개면 트리거도 여러 번 실행된다"는 **문서에 없는 동작을 단정한 창작**을 잡아냄. 둘 다 원문 대조 후 코디네이터가 직접 교정→재조립. 별도로 코디네이터 자체 검증에서 추석 여행 글의 "청량리역"이 writer가 "서울역"으로 바꿔치기한 것도 발견(claims 는 맞았는데 본문이 근거와 다른 고유명사로 드리프트한 사례 — factchecker 없이 T-계열/hangeut-trend 글을 낼 때는 코디네이터가 지명·역명 같은 고유명사를 직접 원문과 대조해야 한다).
  ⚠ **`blog-seo` 가 찾은 `internalLinks`는 본문에 자동 삽입되지 않는다** — `blog-assemble.mjs`는 이걸 URL 허용목록에만 넣지 `<a>` 링크로 박아주지 않는다(코드 확인). SEO 뒤에 이 사실을 안 알려주면 5편 다 "메타에는 있는데 독자에게 보이는 내부링크 0개"로 나간다 — SEO가 찾은 URL은 코디네이터가 본문 어딘가에 마크다운 링크로 직접 넣어야 한다(단, 그 URL은 이미 허용목록이라 assemble이 막지 않는다).
  DKAPTCHA는 이날도 5편 중 3편(냥이/여행/카페는 통과, n8n은 2회 연속 실패)에서 발생 — 연속 실패 뒤엔 [[tistory-publish-captcha]] 대로 재시도 간격을 두고 사람 개입을 기다렸다.
- [2026-09-13] (블로그/SEO·구조) 사용자가 애드센스 수익($0.01/일)을 보고 구글 서치콘솔을 직접 확인시켜본 결과, **미색인 613개 중 195개가 "적절한 표준 태그가 포함된 대체 페이지"(중복/유사 판정)**, 369개가 "발견됨-크롤링 안 됨"(구글이 크롤 우선순위를 낮게 봄), 30개가 "크롤링됨-색인 안 됨"(품질 거부)이었다. 클릭 상위 10페이지가 전체 클릭의 80%를 가져가는데 전부 파이프라인 이전(2023~2024)의 옛 개별 글이고, 지난 두 달 파이프라인 신작 150여 편은 클릭 0. **원인 하나를 실측으로 확인**: hangeut-trend 카테고리(전체 발행의 62%, 95/153편)가 소제목·마무리 문장을 구조 문서 예시 그대로 반복해왔고, 같은 날 쓴 2편도 지시 없이 자연히 "[날짜] 기준으로 정리했어요 … 다음 글에서는 ~ 다뤄볼게요" 마무리 공식으로 수렴했다(LLM에게 "다르게 써라"고만 하면 안 지켜진다 — hangeut-trust.md엔 2026-09-03에 이미 같은 지적이 있었는데 hangeut-trend.md엔 반영이 안 돼 있었다).
  **기계 게이트로 대응**(`backend/scripts/blog-assemble.mjs`, `backend/blog/structure-log.json`에 카테고리별 최근 글의 소제목·마무리 패턴을 기록): 같은 카테고리 최근 글과 소제목 3개 이상이 토씨까지 같으면 **조립을 막고**(실측 확인됨), 마무리 상투구가 최근 글 절반 이상과 겹치면 경고만(CTA 폴백 자체는 카테고리 프로파일이 승인한 정당한 방식이라 막지는 않음). `hangeut-trend.md`·`news.md`·`listicle.md`·`00_common.md`에 슬롯별 표현 로테이션 예시를 추가. 일일 배치는 이 진단 전에 이미 중단 결정([[blog-monetization-reset-2026-09]])했었는데, 숫자로 원인이 구체화됨.
- [2026-09-14] (블로그/SEO·재개준비) 일일 배치 중단([[blog-monetization-reset-2026-09]]) 재개 조건 2 "검색수요 게이트"를 **실제 도구로 구현**: `backend/scripts/keyword-demand.mjs` — 네이버 검색광고 키워드도구 API(`api.searchad.naver.com/keywordstool`, HMAC-SHA256 서명 헤더 X-Timestamp/X-API-KEY/X-Customer/X-Signature)로 주제의 **월간 검색수 절대값**(PC+모바일)을 조회, `--min`(기본 100) 미달이면 exit 1 로 그 주제를 막는다. `--selftest`(parseCount·norm·sign 검증) 통과. seo-playbook §1·blog-run-today §2·CLAUDE.md 스크립트표에 게이트로 연결. **남은 건 자격증명 발급뿐** — searchad.naver.com → API 사용 관리(무료, 광고집행 불필요)에서 3개 키를 받아 `.env`(gitignore)에 `NAVER_AD_API_KEY`·`NAVER_AD_SECRET`·`NAVER_AD_CUSTOMER` 로 넣으면 자동 동작(Node 24 `process.loadEnvFile`로 로드). 네이버는 저검색량을 `"< 10"` 문자열로 주므로 parseCount가 5로 강등. 재개 조건 1(색인)은 GSC 09-13 진단 완료·네이버 서치어드바이저 등록은 사람 로그인 필요(seo-playbook §3), 조건 3(사용자 재개 명시)은 여전히 미충족 — 게이트만 준비됨, 배치는 계속 중단.
- [2026-09-14] (블로그/발행) 재산세 9월 납부 글 1편을 조사→집필→blog-factchecker(통과)→조립까지 끝냈으나 **발행이 DKAPTCHA(티스토리 지도 봇검증)로 3연속 실패**. blog-publish는 발행 클릭 후 30초 안에 글주소로 이동하길 기다리는데(라인 714), 그 사이 뜬 지도 캡차를 **아무도 안 풀어** 시간초과. 시험삼아 30초→180초로 늘려도 실패 → **원인은 대기시간이 아니라 사람이 화면 앞에 없었던 것**(사용자가 다른 기기에서 대화만 보는 중). 그래서 180초 변경은 무인 실행만 느리게 해 되돌림([[tistory-publish-captcha]] "30초창서 직접풀기"가 맞는 설계 — 단 사람이 현장에 있어야 성립). 교훈 두 가지: (1) 발행은 사람이 그 PC 화면 앞에 있을 때만 가능 — 원격/다른기기 세션에선 검증까지만 하고 발행은 넘긴다. (2) 실패해도 **재시도를 반복하지 마라** — 매 시도가 티스토리에 임시저장 초안을 하나씩 남겨(오늘 08:17·08:20 등 재산세 초안 여러 개 생성) 봇검증도 악화된다. 산출물은 out/blog(gitignore)에 있고 posted.json은 성공 시에만 기록되므로 중복 발행 위험은 없다.
- [2026-09-14] (블로그/발행·후속) 5편 배치(재산세+수능·억새·김장·축의금, 전부 blog-factchecker 통과)를 발행하려 했으나 **DKAPTCHA에서 5회 연속 실패**. 사용자가 "내가 캡차 풀게"라고 했고 대기시간을 30초→180초(3분)로 늘려도 실패 — 스크린샷 확인 결과 글은 에디터에 완전히 채워졌고 지도 캡차만 답변칸 빈 채 떠 있었다. 즉 **그 PC의 Playwright 창에서 실제 조작이 안 일어남**(사용자가 다른 기기에서 대화만 보거나, 창이 포커스를 못 받는 상황). 결론: (1) blog-publish 자동 발행은 사람이 **그 Windows 화면 앞에서 창을 직접 조작**해야만 성립 — 원격/다른기기에선 불가. (2) 더 신뢰되는 길은 **사용자가 자기 브라우저에서 티스토리 임시저장 초안을 직접 발행**(관리>글>저장된 글, 어느 기기든·시간 무제한). (3) 180초로 늘려도 사람이 없으면 소용없고 무인 실행만 3분 지연 → 커밋 안 함(원복). (4) 실패마다 초안이 쌓이니(재산세 4개+, 수능 등) 반복 시도 금물 — 정리 필요. 5편 산출물은 out/blog(gitignore), posted.json 미기록. 조사 4건은 커밋함.
- [2026-09-14] (블로그/발행·해결) 위 5편 배치, 사용자가 그 PC 앞에서 직접 캡차를 풀자 **전부 발행 성공**(수능 /435·억새 /436·김장 /437·축의금 /438·재산세 /439). 결정적 요인은 **발행 확인 대기 30초 → 180초(3분)**: 30초는 사람이 지도 DKAPTCHA를 읽고 답을 넣기에도 짧아 앞서 5회 연속 실패했는데, 3분으로 늘리자 5편 연속 통과. 발행은 어차피 사람이 붙어 있는 반자동 단계(무인 자동화 안 함)라 slower-failure 단점이 무의미 → `blog-publish.mjs` 대기값을 **180초로 정식 반영·커밋**(이전 09-14 항목의 "원복"을 이 근거로 뒤집음). 부수 사실: 초안이 쌓이면 "이어서 작성?" 다이얼로그가 뜨는데 accept돼도 setContent로 본문을 덮어써 **정상 내용으로 게시됨**(억새에서 확인). permalink이 /manage/posts/로 남으면 RSS(제목 매칭)로 /NNN 찾아 posted.json 교정 — 5편 중 3편이 그랬고 전부 교정함. 발행 실패로 쌓인 재산세·수능 임시저장 초안은 사용자가 정리 필요.
- [2026-09-16] (블로그/중복·주제선정) 사용자 "그냥 5편 배치 돌려" 재개 배치에서 **`backend/blog/research/*.json` 을 재사용하려다 4편이 기발행 중복이었다**(kbo-postseason→/413·october-2026-changes→/409·flu-vaccine→/408·autumn-seafood→/393, 전부 09-06~09 발행). 원인: 조사 파일이 남아있는 건 대개 **그 조사로 이미 글을 냈기 때문** — 미발행 백로그가 아니다. 게다가 첫 dedup grep을 `has()[0]`만 출력하게 짜서 /413 매치를 **눈으로 놓쳤다**(여러 매치 중 첫 건만 보임). 결정적 체크: **`backend/out/blog/posted.json` 은 slug 키 맵**(`{ "<slug>": {url,at,title} }`)이라, 쓰기 전에 `node -e 'require("./backend/out/blog/posted.json")["<slug>"]'` 로 **slug 존재 여부를 먼저 본다**(배열로 오해해 Object.values 하면 키가 날아가 전부 "미발행"으로 오판). blog-publish 도 `posted[id]` 로 막지만 그건 집필·검증·팩트체크를 다 태운 뒤라 낭비. 반대로 blog-factchecker 가 준중복(/393)을 **주제 단계에서 놓친 걸 사후에 잡아준 것**은 유효(warn). 교훈: 재사용 조사는 반드시 posted.json[slug] 미존재를 먼저 확인하고, 없는 것만 쓴다 — 사실상 백로그는 소진 상태이므로 신규 주제는 그날 WebSearch 조사가 기본. [[feedback-blog-pick-trend-dedup]]
  ⚠ 이날 **티스토리 로그인 세션 만료**로 발행 전부 불가(첫 발행 시도가 `auth/login` 리다이렉트) — 카카오 로그인 자동화 금지 철칙대로 검증까지만 하고 발행은 사람 로그인(`node backend/scripts/blog-publish.mjs --login`, "로그인 상태 유지" 체크) 뒤로 넘겼다.
- [2026-09-19] (블로그/자동화) `aztomz-blog-daily` 크론이 `claude.cmd`의 `Failed to authenticate: OAuth session expired` 오류로 실패함을 재확인. 스킬 문서에 명시된 비대화형 셸의 CLI 인증 한계가 현재 블로커임.
- [2026-09-17] (블로그/중복·주제선정) 사용자 "오늘 작업 진행"→"발행시작"으로 배치 중단([[blog-monetization-reset-2026-09]]) 재개. 처음 고른 3편(추석 차례상·통행료·가을 해산물)을 **최근 25편 제목만 대조**해 통과시켰다가 조사·집필·조립까지 끝낸 뒤 발행 직전에야 전부 기발행 중복으로 판명(/407·/411·/393 — 추석 클러스터는 /376~423에 16편+ 이미 소진). 교훈: 시즌 주제는 정점 몇 주 전에 이미 다뤄졌을 확률이 높으니 **최근 N편이 아니라 posted.json 제목 전체를 개념 단위 정규식 grep**으로 대조해야 한다(slug 존재 확인만으론 부족 — 슬러그 달라도 주제 동일 가능). 재선정으로 확인된 빈틈 5편 발행 성공: 전기장판·온수매트 /450, 하객룩 /451, 핼러윈 의상 /452, 겨울 이불 /453, 결로·곰팡이 /454 — 전부 그날 WebSearch 실조사(각 7~11 출처, 공공/언론 우선). `claude -p`(selfreview·verify)는 이 환경에서 호출 불가(claude.cmd 실패)라 코디네이터가 조사 JSON과 본문을 직접 대조 검증. DKAPTCHA는 5편 모두 사용자가 그 PC에서 직접 풀어 통과([[tistory-publish-captcha]]). [[feedback-blog-pick-trend-dedup]]
- [2026-09-23] (소스/검증) '1020룸 (을지로)' 수집 시, 출처 제목이 '을지로 카페' 같은 일반 키워드로만 구성되어 가게 이름이 없어 '무관'으로 자동 보류됨. 출처 제목에 분석 대상의 고유명사가 명확히 포함되어야 자동 검증을 통과할 수 있음을 재확인.
- [2026-09-23] (운영) `auto-build.mjs` 실행 중 `pull --rebase` 실패로 git push가 보류됨. 다른 자동화 작업(광고/진짜 일일 재확인)과 거의 동시에 실행되어 git 충돌이 발생한 것으로 추정. 로컬 데이터는 업데이트되었으나 사이트 배포는 수동 조치가 필요함.
- [2026-09-23] (블로그/병렬화·가드) 여행 공백 13편 배치를 **Workflow 3단 파이프라인**(조사→집필→팩트체크, 서브에이전트 39개)으로 돌려 전편 blocker 0. 두 가지가 실제로 먹혔다. (1) **소제목 충돌 선제 배분** — `blog-assemble` 의 구조중복 게이트는 같은 카테고리 최근 8편과 h2 3개 이상 일치 시 차단인데, 거의 모든 글이 "정리"·"참고"를 쓰므로 **이미 2개가 자동으로 겹친다**. 세 번째만 겹쳐도 막히므로, 13편에 마무리 h2 변형("한눈에 정리"/"핵심 요약"/"요점만 다시"…, "참고 자료"/"출처"/"근거 자료"…)을 미리 하나씩 배분해 충돌을 0으로 만들었다. (2) **병렬 assemble 은 서로를 못 본다** — `structure-log.json` 이 read-modify-write 라 동시 실행분끼리는 교차검사가 안 된다. 발행 전 **순차 재조립 1회**로 교차검사를 다시 돌려야 한다(이번엔 전부 통과). [[feedback-blog-quality-cadence]]
- [2026-09-23] (블로그/발행) DKAPTCHA 로 실패한 편은 **재시도 전에 RSS + posted.json 으로 실제 발행 여부를 먼저 확인한다** — "발행 후 이동을 확인하지 못했습니다"는 캡차 미해결일 수도, 이동 감지 실패(실제로는 발행됨)일 수도 있어 바로 재시도하면 중복 발행 위험이 있다. 이번엔 둘 다 없어 미발행 확정 후 재시도해 /473 성공. 제목에 가운뎃점('·')을 쓰지 않으면 RSS permalink 조회가 깨지지 않아 13편 전부 /NNN 이 바로 잡혔다([[tistory-publish-captcha]] 의 permalink 교정 수작업이 불필요해짐).
- [2026-09-23] (운영/git) 밤 자동화(`auto-build.mjs`)의 `pull --rebase` 실패로 로컬 main 이 2커밋 미푸시 + origin 8커밋 미반영으로 갈라져 **라이브 사이트가 정체**돼 있었다(철칙의 그 증상). 충돌은 `generatedAt` 한 줄뿐 — 수동 rebase → `validate-trends` → `refresh` → push 로 복구. 또 봇이 learnings.md 에 항목을 추가하며 **기존 2026-09-08 항목의 이어지는 줄을 통째로 덮어썼다**(`|-` 접두어 깨짐과 같은 증상의 더 나쁜 버전) — 봇 append 후에는 접두어뿐 아니라 **삭제된 줄이 있는지도 `git diff` 로 확인**할 것.
- [2026-09-23] (분야/최신성) 카페·핫플 수집 시 "요즘 유행 카페 컨셉/이색/2026" 같은 직접 검색은 결과 0이 잦았다(ddgs "No results found"). 뉴스 채널(`ddgs news`)로 전환하니 차지(CHAGEE) 밀크티 확장 기사가 풍부하게 잡힘 — 브랜드/체인점형 카페 트렌드는 일반 블로그보다 뉴스 검색이 더 잘 잡힌다.
- [2026-09-23] (소스/검증) `auto-build.mjs`는 "신촌 카페 차지 밀크티 내돈내산 후기"처럼 제목에 수식어가 많으면 핵심어(차지/CHAGEE)가 있어도 '무관(일반어뿐)'으로 보류시켰다(뉴스·보도자료성 제목 3건은 통과). 블로그 후기 출처는 제목이 짧고 브랜드명이 앞에 나오는 것을 우선 고를 것.
- [2026-09-24] (블로그/중복·주제선정) 주제 후보 중복 검사를 **고정된 키워드 목록으로 posted 제목을 grep** 했더니 그 목록에 없던 '노벨'을 놓쳐 이미 /446 으로 나간 "노벨상 발표 일정"을 조사·집필까지 진행했다(SEO 에이전트가 발견). 중복 검사는 **후보마다 그 주제의 핵심 명사 자체로** posted.json 제목·슬러그를 grep 한다. 또 행사형 주제는 조사 전에 **올해 개최일이 이미 지났는지**부터 본다(서울세계불꽃축제 2026 은 9/5 종료 — 조사 후에야 알았다). [[blog-run-today-worktree-dedup]]
- [2026-09-24] (블로그/조사·단위) 조사 에이전트가 영문 "42 billion parameters" 를 "42억"으로 옮겼다(정답 420억). selfreview 가 Flash(3,100억 중 150억)와의 비율이 어색하다고 짚어 원문 WebFetch 로 확정·교정. 영문 수치의 billion/trillion 환산은 원문 대조 대상이고, 같은 문장이 **전작 모델 설명**이었던 것도 함께 잡혔다 — "was already" 류 시제도 볼 것.
- [2026-09-24] (네이버 블로그) `blog-publish-naver.mjs` 실측: SmartEditor ONE 은 **합성 paste 이벤트를 무시**하고 진짜 클립보드+Ctrl+V 만 받는다(표·소제목·링크는 변환됨, iframe은 버려짐→링크로 치환). 창을 화면 밖(`--window-position=-32000,-32000`)에 띄워도 Playwright 포커스 에뮬레이션 덕에 클립보드 동작함. 네이버 카테고리 관리(`AdminCategoryView.naver`)는 저장이 `parent.layeredAlert.doSubmit` 이라 **단독으로 열면 확인 버튼이 조용히 무시**된다 — `AdminMain.naver?Redirect=Categoryinfo` 안의 프레임에서 조작해야 저장됨. 네이버 로그인 쿠키(NID_AUT/SES)는 '로그인 상태 유지' 시 30일 영구 쿠키라 티스토리처럼 keepalive 불필요.
